import { IndividualQuizCard } from "@/app/components/quiz/card/quizcard";
import prisma from "@/lib/db";

export default async function QuizExam({ params }: { params: { id: string } }) {

    return (
        <>
          THIS IS CHILD PORN
        </>
    );
  }